import React from 'react';
import { storiesOf } from '@kadira/react-native-storybook';
import POICallout from './POICallout';

storiesOf('POICallout')
  .add('default', () => (
    <POICallout />
  ));
