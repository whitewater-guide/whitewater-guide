enum Role {
  ADMIN
  EDITOR
  PREMIUM
  USER
}

directive @auth(requires: Role) on FIELD_DEFINITION

scalar JSON
scalar Date

enum SortDirection {
  asc
  desc
}

input RefInput {
  id: ID!
  name: String
}

input Page {
  limit: Int
  offset: Int
}

type UploadLink {
  postURL: String!
  formData: JSON!
  key: String!
}

schema {
  query: Query
  mutation: Mutation
  # subscription: Subscription
}
