type Measurement {
  timestamp: Date
  level: Float
  flow: Float
}

type HarvestStatus {
  count: Int
  success: Boolean
  timestamp: Date
}

type Query {
  lastMeasurements(gaugeId: ID, sectionId: ID, days: Int!): [Measurement]
}
