enum TagCategory {
  kayaking,
  hazards,
  supply,
  misc,
}

type Tag {
  id: String!
  name: String!
  category: TagCategory!
}

input TagInput {
  id: String!
  name: String!
  category: TagCategory!
}

type Query {
  tags: [Tag!]!
}

type Mutation {
  upsertTag(tag: TagInput!): Tag @admin
  removeTag(id: String!): String @admin
}
