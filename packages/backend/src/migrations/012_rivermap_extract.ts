/**
 * This loads rivermap.ch database dump
 */
export const up = async () => {
  // Data migrations are no longer available is source code
};

export const down = async () => {
  // Data migrations are no longer available is source code
};

export const configuration = { transaction: true };
