import { InjectedFormProps } from 'redux-form';
import { MediaInput } from '../../../ww-commons';

export type MediaFormInput = MediaInput;
export type MediaFormProps = InjectedFormProps<MediaFormInput>;
