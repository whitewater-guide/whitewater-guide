query listMyDescents($filter: DescentsFilter, $page: Page) {
  myDescents(filter: $filter, page: $page) {
    edges {
      node {
        ...DescentCore

        section {
          ...SectionNameShort
        }
      }
      cursor
    }
    pageInfo {
      endCursor
      hasMore
    }
  }
}
