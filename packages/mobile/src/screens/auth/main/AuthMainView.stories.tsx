import { storiesOf } from '@storybook/react-native';
import React from 'react';
import { AuthMainView } from './AuthMainView';

storiesOf('AuthMainView', module).add('Default', () => <AuthMainView />);
