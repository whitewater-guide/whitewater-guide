import * as React from 'react';

const styles = {
  container: {
    width: '100%',
    height: '100%',
    display: 'flex',
    padding: 24,
  },
};

export const SourcesList: React.StatelessComponent = () => (
  <div style={styles.container}>
    <h1>Sources list</h1>
  </div>
);
