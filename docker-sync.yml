version: '2'

options:
  compose-file-path: 'build/docker-stack-development.yml'
syncs:
  ww-backend-sync:
    src: './packages/backend/'
    notify_terminal: true
    sync_excludes: ['Dockerfile.*', 'tsconfig.json', 'tslint.json', 'bin', 'node_modules', '*.ts', '*.iml']
    watch_excludes: ['Dockerfile.*', 'tsconfig.json', 'tslint.json', 'bin', 'node_modules', '*.ts', '*.iml']
