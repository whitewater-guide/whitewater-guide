import region from './region';
import regionEditors from './regionEditors';
import regionMediaForm from './regionMediaForm';
import regions from './regions';

export default {
  region,
  regions,
  regionEditors,
  regionMediaForm,
};
