{$ADMINER_ADDRESS} {
  import ../caddy-common.conf

  header / {
    X-Robots-Tag noindex
  }

  proxy / adminer:8080 {
    transparent
  }

  basicauth {$ADMINER_USERNAME} {$ADMINER_PASSWORD} {
    realm {$ADMINER_ADDRESS}
    /
  }
}
