version: '3.5'

x-single-host:
  &ww-single-host
  deploy:
    placement:
      constraints: [node.role == manager]
  labels:
    guide.whitewater: ""

services:
  backend:
    <<: *ww-single-host
    env_file:
      - ${DOCKER_ENV_FILE}
    build:
      context: ../packages
      labels:
        guide.whitewater: ""
    depends_on:
      - db
      - minio
  db:
    <<: *ww-single-host
    env_file:
      - ${DOCKER_ENV_FILE}
    build:
      context: ../packages/db
      labels:
        guide.whitewater: ""
    read_only: true
  minio:
    <<: *ww-single-host
    env_file:
      - ${DOCKER_ENV_FILE}
    read_only: true
  imgproxy:
    <<: *ww-single-host
    env_file:
      - ${DOCKER_ENV_FILE}
    image: 'darthsim/imgproxy:latest'
    read_only: true
  caddy:
    <<: *ww-single-host
    env_file:
      - ${DOCKER_ENV_FILE}
    build:
      context: ../packages
      dockerfile: caddy/Dockerfile
      labels:
        guide.whitewater: ""
    read_only: true
