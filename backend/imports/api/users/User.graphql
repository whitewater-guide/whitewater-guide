type User {
  _id: ID!,
  email: String!,
  profile: UserProfile,
  roles: [String]!,
}

type UserProfile {
  #Facebook id
  id: String,
  name: String,
  link: String,
}

type Query {
  me: User,
  listUsers: [User]!,
}

type Mutation {
  toggleAdmin(_id: ID!, isAdmin: Boolean!):User,
}