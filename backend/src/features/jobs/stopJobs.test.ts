// import { cancelJob } from 'node-schedule';
// import { stopJobs } from './stopJobs';
//
// jest.mock('node-schedule', () => ({
//   ...require.requireActual('node-schedule'),
//   cancelJob: jest.fn(),
// }));
//
// it('should call schedule stop for all-at-once sources', () => {
//   stopJobs('galicia');
//   expect(cancelJob).toBeCalledWith('galicia');
// });
//
// it('should call schedule stop for one-by-one gauges', () => {
//   stopJobs('galicia', 'gal1');
//   expect(cancelJob).toBeCalledWith('galicia::gal1');
// });
it.skip('test', () => {

});
